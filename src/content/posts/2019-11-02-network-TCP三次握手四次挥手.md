---
title: "TCP协议"
date: "2019-11-02T11:42:29.000Z"
description: "TCP首部格式 源端口和目的端口:各占两个字节，分别写入源端口号和目的顿口号 序号seq：占四个字节，范围是【0, 2^32 - 1】 共 2^32个序号，在TCP字节流中，每个字节都会按顺序编号，本字段表示本报文段中所发送的数据的第一个字节的序号。 确认号ack：期望收到对方下一个报文段的第一个数"
legacyPath: "2019/11/02/network-TCP三次握手四次挥手"
tags:
  - "计算机网络"
tagSlugs:
  - "计算机网络"
---
## TCP首部格式

![tip](/images/TCP%E4%B8%89%E6%AC%A1%E6%8F%A1%E6%89%8B%E5%9B%9B%E6%AC%A1%E6%8C%A5%E6%89%8B/TCP%E9%A6%96%E9%83%A8%E6%A0%BC%E5%BC%8F.png)

-   源端口和目的端口:各占两个字节，分别写入源端口号和目的顿口号
-   序号seq：占四个字节，范围是【0, 2^32 - 1】 共 2^32个序号，在TCP字节流中，每个字节都会按顺序编号，本字段表示本报文段中所发送的数据的第一个字节的序号。
-   确认号ack：期望收到对方下一个报文段的第一个数据字节的编号。若确认号文N，则证明N-1之前的数据都已经正确收到。
-   数据偏移：即首部长度，以4B为单位，即一个数值为4B
-   保留：
-   URG：紧急位，当URG=1时，标明此报文段中有紧急数据传送，是优先级最高的数据，不需要在缓存队列中排队，配合紧急指针字段使用。
-   ACK：确认位，ACK=1时确认号有效，在连接建立后传送的所有报文段ACK都必须置为1.
-   PSH：推送位，PSH=1时，接收方应尽快交付接受的应用进程，不用再等到缓存填满再想上层交付。
-   RST：复位，RST=1时说明TCP连接出现严重错误，必须释放连接然后重新建立。
-   SYN：同步位，SYN=1时，表明是一个连接请求/连接接受报文。
-   FIN：FIN=1时，表明此报文段发送方数据已发完，请求释连接。
-   窗口：指的是发送本报文段的一方的接受窗口，即现在允许对方发送的数据量。
-   检验和：检验首部+数据，检验时要加上12B伪首部，第四个字段为6.
-   紧急指针：URG=1时才有意义，指出本报文段中紧急数据的字节数。
-   选项：长度可变
-   填充：
    
    ## 三次握手
    
    握手是为了建立连接，TCP三次握手的流程如下：  
    

1.  第一次握手：建立连接时，客户端发送SYN包到服务器，并进入SYN\_SEND状态，等待服务器确认；
2.  第二次握手：服务器收到SYN包，必须确认客户的SYN同时自己也发送一个SYN包，即SYN+ACK包，此时服务器进入SYN\_RECV状态；
3.  第三次握手：客户端收到服务器的SYN+ACK包，向服务器发送确认包ACK，此包发送完毕，客户端和服务器进入ESTABLISHED状态，完成三次握手。
    
    ## 四次挥手
    
    “挥手”是为了能终止连接，TCP四次挥手的流程如下：  
    
4.  第一次挥手：Client发送一个FIN，用来关闭Client到Server的数据传送，Client进入FIN\_WAIT\_1的状态；
5.  第二次挥手：Server收到FIN后，发送一个ACK给Client，确认序号为收到序号+1（与SYN相同，一个FIN占用一个序号），Server进入CLOSE\_WAIT状态；
6.  第三次挥手：Server发送一个FIN，用来关闭Server到Client的数据发送，Server进入LAST\_ACK状态；
7.  第四次挥手：Client收到FIN后，Client进入TIME\_WAIT状态，接着发送一个ACK给Server，确认序号为收到序号+1，Server进入CLOSED状态，完成四次挥手。
    
    ### 为什么会有TIME\_WAIT状态
    

-   确保有足够时间让对方收到ACK包
-   避免新旧连接混淆
    
    ## 为什么需要四次挥手才能断开连接
    
    因为全双工，发送方和接收方都需要FIN报文和ACK报文，各需要两次挥手
    
    ## 服务器出现大量CLOSE
    

## TCP滑动窗口

### RTT和RTO

-   RTT：发送一个数据包收到对应的ACK，所花费的时间。
-   RTO：重传时间间隔。根据RTT计算。
    
    ### TCP的滑动窗口
